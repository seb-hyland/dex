//! Proc macro that implements consistent codegen for types that should be shared with dynamic scripting languages.

use proc_macro::TokenStream;

pub fn dynamic_impl(attr_tokens: TokenStream, body_tokens: TokenStream) -> TokenStream {
    todo!()
}
